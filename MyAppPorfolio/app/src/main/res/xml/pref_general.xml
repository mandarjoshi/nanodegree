<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="popularity.desc"
        android:entries="@array/pref_movie_sort_by_title"
        android:entryValues="@array/pref_movie_sort_by_values"
        android:key="@string/movie_sort_by"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_sort_by" />

</PreferenceScreen>
